# include <iostream>
using namespace std; 
//动态数组：我们定义数组的时候往往已经指定了数组的长度， 有时候我们需要用到动态数组，数组的长度根据需要可以发生变化，可以用指针实现这一目的。
//可以定义一个指针变量p，然后new一段空间并将其赋值给 p
int main(){
	//将字符串赋值给字符指针	
	char str[] = "stringstringstring";
	string s = "str";
	
	for(int i=0;i<)
	 
} 
